# Pregnancy weight gain chart

## Interaction updates

- the chart updates automatically as the user edits required and optional fields
- past measurements use an add-row interface instead of a comma-separated textarea
- the footer includes attribution with a link to the creator's personal page
